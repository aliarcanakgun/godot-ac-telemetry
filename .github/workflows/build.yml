name: Build GDExtension

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows
            os: windows-latest
            arch: x86_64

    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install SCons
        run: pip install scons

      - name: Build (debug)
        run: scons platform=${{ matrix.platform }} arch=${{ matrix.arch }} target=template_debug

      - name: Build (release)
        run: scons platform=${{ matrix.platform }} arch=${{ matrix.arch }} target=template_release

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: godot-sim-telemetry-${{ matrix.platform }}-${{ matrix.arch }}
          path: demo/addons/godot-sim-telemetry/

  package:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Merge into single addon folder
        run: |
          mkdir -p release/addons/godot-sim-telemetry
          for dir in artifacts/godot-sim-telemetry-*/; do
            rsync -a "$dir"/ release/addons/godot-sim-telemetry/
          done
          find release/addons/godot-sim-telemetry -name "*.exp" -type f -delete
          find release/addons/godot-sim-telemetry -name "*.lib" -type f -delete

      - name: Create release ZIP
        run: |
          ZIP_NAME="godot-sim-telemetry-${{ github.ref_name }}.zip"
          cd release && zip -r ../$ZIP_NAME addons/
          echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_ENV

      - name: Upload release ZIP
        uses: actions/upload-artifact@v4
        with:
          name: godot-sim-telemetry-release
          path: ${{ env.ZIP_NAME }}

      - name: Publish to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ env.ZIP_NAME }}
          generate_release_notes: true
